# Eval seeds

Three seed cases for `claude plugin eval`, which is in early access; none of these has yet executed against the real runner. The prompts reference `examples/legacy-shop/...` relative to the repo root, so run the command from there once it is available.
